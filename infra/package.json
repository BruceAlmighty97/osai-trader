{
	"name": "osai-trader-infra",
	"version": "0.1.0",
	"private": true,
	"description": "AWS CDK infrastructure for OsaiTrader (ECS Fargate + RDS + Secrets Manager)",
	"scripts": {
		"build": "tsc --noEmit",
		"synth": "cdk synth",
		"diff": "cdk diff",
		"deploy": "cdk deploy"
	},
	"dependencies": {
		"aws-cdk-lib": "^2.170.0",
		"constructs": "^10.4.2"
	},
	"devDependencies": {
		"aws-cdk": "^2.1000.0",
		"@types/node": "^22.10.7",
		"ts-node": "^10.9.2",
		"typescript": "^5.7.3"
	}
}
